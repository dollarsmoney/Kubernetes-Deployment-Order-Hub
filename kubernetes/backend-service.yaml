apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: jollof-run
  labels:
    app: backend
    app.kubernetes.io/part-of: jollof-run
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: http
