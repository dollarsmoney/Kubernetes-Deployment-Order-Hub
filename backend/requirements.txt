# Pinned exactly. An unpinned requirements.txt means "this image builds
# differently every day", which makes a container image unreproducible and
# makes "it worked last week" impossible to investigate.

fastapi==0.115.6
uvicorn[standard]==0.34.0

# SQLAlchemy Core. We use it for its connection pooling and its bound-parameter
# API, not as an ORM — the queries in db.py are plain SQL.
SQLAlchemy==2.0.36

# PostgreSQL driver. The -binary wheel bundles libpq so the image needs no
# build toolchain and no libpq-dev. Smaller image, faster build.
psycopg2-binary==2.9.10

# Only used by the ALTERNATIVE credential path in db.py (direct Secrets Manager
# fetch). The primary path reads the file the CSI driver mounts and needs no
# AWS SDK at all.
boto3==1.35.90
